/*
 * Daytona Toolbox API
 * Daytona Toolbox API. The base URL comes from the sandbox's `toolboxProxyUrl` field (returned in sandbox DTO by the main Daytona API) plus the sandbox ID: `{toolboxProxyUrl}/{sandboxId}/{endpoint}`. Default for Daytona Cloud: `https://proxy.app.daytona.io/toolbox/{sandboxId}`.
 *
 * The version of the OpenAPI document: v0.0.0-dev
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package io.daytona.toolbox.client.model;

import java.util.Objects;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import io.daytona.toolbox.client.model.ProcessLogFrame;
import java.io.IOException;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonArray;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.TypeAdapter;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;

import java.util.HashMap;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Set;

import io.daytona.toolbox.client.JSON;

/**
 * ProcessLogPage
 */
@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", comments = "Generator version: 7.21.0")
public class ProcessLogPage {
  public static final String SERIALIZED_NAME_EOF = "eof";
  @SerializedName(SERIALIZED_NAME_EOF)
  @javax.annotation.Nullable
  private Boolean eof;

  public static final String SERIALIZED_NAME_FIRST_AVAILABLE_CURSOR = "firstAvailableCursor";
  @SerializedName(SERIALIZED_NAME_FIRST_AVAILABLE_CURSOR)
  @javax.annotation.Nullable
  private String firstAvailableCursor;

  public static final String SERIALIZED_NAME_FRAMES = "frames";
  @SerializedName(SERIALIZED_NAME_FRAMES)
  @javax.annotation.Nonnull
  private List<ProcessLogFrame> frames = new ArrayList<>();

  public static final String SERIALIZED_NAME_NEXT_CURSOR = "nextCursor";
  @SerializedName(SERIALIZED_NAME_NEXT_CURSOR)
  @javax.annotation.Nullable
  private String nextCursor;

  public static final String SERIALIZED_NAME_TRUNCATED_HEAD = "truncatedHead";
  @SerializedName(SERIALIZED_NAME_TRUNCATED_HEAD)
  @javax.annotation.Nullable
  private Boolean truncatedHead;

  public ProcessLogPage() {
  }

  public ProcessLogPage eof(@javax.annotation.Nullable Boolean eof) {
    this.eof = eof;
    return this;
  }

  /**
   * EOF is true when the process is terminal and every retained frame has been returned.
   * @return eof
   */
  @javax.annotation.Nullable
  public Boolean getEof() {
    return eof;
  }

  public void setEof(@javax.annotation.Nullable Boolean eof) {
    this.eof = eof;
  }


  public ProcessLogPage firstAvailableCursor(@javax.annotation.Nullable String firstAvailableCursor) {
    this.firstAvailableCursor = firstAvailableCursor;
    return this;
  }

  /**
   * Get firstAvailableCursor
   * @return firstAvailableCursor
   */
  @javax.annotation.Nullable
  public String getFirstAvailableCursor() {
    return firstAvailableCursor;
  }

  public void setFirstAvailableCursor(@javax.annotation.Nullable String firstAvailableCursor) {
    this.firstAvailableCursor = firstAvailableCursor;
  }


  public ProcessLogPage frames(@javax.annotation.Nonnull List<ProcessLogFrame> frames) {
    this.frames = frames;
    return this;
  }

  public ProcessLogPage addFramesItem(ProcessLogFrame framesItem) {
    if (this.frames == null) {
      this.frames = new ArrayList<>();
    }
    this.frames.add(framesItem);
    return this;
  }

  /**
   * Get frames
   * @return frames
   */
  @javax.annotation.Nonnull
  public List<ProcessLogFrame> getFrames() {
    return frames;
  }

  public void setFrames(@javax.annotation.Nonnull List<ProcessLogFrame> frames) {
    this.frames = frames;
  }


  public ProcessLogPage nextCursor(@javax.annotation.Nullable String nextCursor) {
    this.nextCursor = nextCursor;
    return this;
  }

  /**
   * NextCursor resumes strictly after the last returned frame. It is set even when Frames is empty so pollers can keep a stable position.
   * @return nextCursor
   */
  @javax.annotation.Nullable
  public String getNextCursor() {
    return nextCursor;
  }

  public void setNextCursor(@javax.annotation.Nullable String nextCursor) {
    this.nextCursor = nextCursor;
  }


  public ProcessLogPage truncatedHead(@javax.annotation.Nullable Boolean truncatedHead) {
    this.truncatedHead = truncatedHead;
    return this;
  }

  /**
   * TruncatedHead reports that the oldest frames of this process were evicted under a byte cap. Never silently skip: this is the honest signal.
   * @return truncatedHead
   */
  @javax.annotation.Nullable
  public Boolean getTruncatedHead() {
    return truncatedHead;
  }

  public void setTruncatedHead(@javax.annotation.Nullable Boolean truncatedHead) {
    this.truncatedHead = truncatedHead;
  }

  /**
   * A container for additional, undeclared properties.
   * This is a holder for any undeclared properties as specified with
   * the 'additionalProperties' keyword in the OAS document.
   */
  private Map<String, Object> additionalProperties;

  /**
   * Set the additional (undeclared) property with the specified name and value.
   * If the property does not already exist, create it otherwise replace it.
   *
   * @param key name of the property
   * @param value value of the property
   * @return the ProcessLogPage instance itself
   */
  public ProcessLogPage putAdditionalProperty(String key, Object value) {
    if (this.additionalProperties == null) {
        this.additionalProperties = new HashMap<String, Object>();
    }
    this.additionalProperties.put(key, value);
    return this;
  }

  /**
   * Return the additional (undeclared) property.
   *
   * @return a map of objects
   */
  public Map<String, Object> getAdditionalProperties() {
    return additionalProperties;
  }

  /**
   * Return the additional (undeclared) property with the specified name.
   *
   * @param key name of the property
   * @return an object
   */
  public Object getAdditionalProperty(String key) {
    if (this.additionalProperties == null) {
        return null;
    }
    return this.additionalProperties.get(key);
  }


  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    ProcessLogPage processLogPage = (ProcessLogPage) o;
    return Objects.equals(this.eof, processLogPage.eof) &&
        Objects.equals(this.firstAvailableCursor, processLogPage.firstAvailableCursor) &&
        Objects.equals(this.frames, processLogPage.frames) &&
        Objects.equals(this.nextCursor, processLogPage.nextCursor) &&
        Objects.equals(this.truncatedHead, processLogPage.truncatedHead)&&
        Objects.equals(this.additionalProperties, processLogPage.additionalProperties);
  }

  @Override
  public int hashCode() {
    return Objects.hash(eof, firstAvailableCursor, frames, nextCursor, truncatedHead, additionalProperties);
  }

  @Override
  public String toString() {
    StringBuilder sb = new StringBuilder();
    sb.append("class ProcessLogPage {\n");
    sb.append("    eof: ").append(toIndentedString(eof)).append("\n");
    sb.append("    firstAvailableCursor: ").append(toIndentedString(firstAvailableCursor)).append("\n");
    sb.append("    frames: ").append(toIndentedString(frames)).append("\n");
    sb.append("    nextCursor: ").append(toIndentedString(nextCursor)).append("\n");
    sb.append("    truncatedHead: ").append(toIndentedString(truncatedHead)).append("\n");
    sb.append("    additionalProperties: ").append(toIndentedString(additionalProperties)).append("\n");
    sb.append("}");
    return sb.toString();
  }

  /**
   * Convert the given object to string with each line indented by 4 spaces
   * (except the first line).
   */
  private String toIndentedString(Object o) {
    return o == null ? "null" : o.toString().replace("\n", "\n    ");
  }


  public static HashSet<String> openapiFields;
  public static HashSet<String> openapiRequiredFields;

  static {
    // a set of all properties/fields (JSON key names)
    openapiFields = new HashSet<String>(Arrays.asList("eof", "firstAvailableCursor", "frames", "nextCursor", "truncatedHead"));

    // a set of required properties/fields (JSON key names)
    openapiRequiredFields = new HashSet<String>(Arrays.asList("frames"));
  }

  /**
   * Validates the JSON Element and throws an exception if issues found
   *
   * @param jsonElement JSON Element
   * @throws IOException if the JSON Element is invalid with respect to ProcessLogPage
   */
  public static void validateJsonElement(JsonElement jsonElement) throws IOException {
      if (jsonElement == null) {
        if (!ProcessLogPage.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
          throw new IllegalArgumentException(String.format(java.util.Locale.ROOT, "The required field(s) %s in ProcessLogPage is not found in the empty JSON string", ProcessLogPage.openapiRequiredFields.toString()));
        }
      }

      // check to make sure all required properties/fields are present in the JSON string
      for (String requiredField : ProcessLogPage.openapiRequiredFields) {
        if (jsonElement.getAsJsonObject().get(requiredField) == null) {
          throw new IllegalArgumentException(String.format(java.util.Locale.ROOT, "The required field `%s` is not found in the JSON string: %s", requiredField, jsonElement.toString()));
        }
      }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
      if ((jsonObj.get("firstAvailableCursor") != null && !jsonObj.get("firstAvailableCursor").isJsonNull()) && !jsonObj.get("firstAvailableCursor").isJsonPrimitive()) {
        throw new IllegalArgumentException(String.format(java.util.Locale.ROOT, "Expected the field `firstAvailableCursor` to be a primitive type in the JSON string but got `%s`", jsonObj.get("firstAvailableCursor").toString()));
      }
      if (jsonObj.get("frames") != null) {
        if (!jsonObj.get("frames").isJsonArray()) {
          throw new IllegalArgumentException(String.format(java.util.Locale.ROOT, "Expected the field `frames` to be an array in the JSON string but got `%s`", jsonObj.get("frames").toString()));
        }
        JsonArray jsonArrayframes = jsonObj.getAsJsonArray("frames");
        // validate the required field `frames` (array)
        for (int i = 0; i < jsonArrayframes.size(); i++) {
          ProcessLogFrame.validateJsonElement(jsonArrayframes.get(i));
        }
      }
      if ((jsonObj.get("nextCursor") != null && !jsonObj.get("nextCursor").isJsonNull()) && !jsonObj.get("nextCursor").isJsonPrimitive()) {
        throw new IllegalArgumentException(String.format(java.util.Locale.ROOT, "Expected the field `nextCursor` to be a primitive type in the JSON string but got `%s`", jsonObj.get("nextCursor").toString()));
      }
  }

  public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
    @SuppressWarnings("unchecked")
    @Override
    public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
       if (!ProcessLogPage.class.isAssignableFrom(type.getRawType())) {
         return null; // this class only serializes 'ProcessLogPage' and its subtypes
       }
       final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
       final TypeAdapter<ProcessLogPage> thisAdapter
                        = gson.getDelegateAdapter(this, TypeToken.get(ProcessLogPage.class));

       return (TypeAdapter<T>) new TypeAdapter<ProcessLogPage>() {
           @Override
           public void write(JsonWriter out, ProcessLogPage value) throws IOException {
             JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
             obj.remove("additionalProperties");
             // serialize additional properties
             if (value.getAdditionalProperties() != null) {
               for (Map.Entry<String, Object> entry : value.getAdditionalProperties().entrySet()) {
                 if (entry.getValue() instanceof String)
                   obj.addProperty(entry.getKey(), (String) entry.getValue());
                 else if (entry.getValue() instanceof Number)
                   obj.addProperty(entry.getKey(), (Number) entry.getValue());
                 else if (entry.getValue() instanceof Boolean)
                   obj.addProperty(entry.getKey(), (Boolean) entry.getValue());
                 else if (entry.getValue() instanceof Character)
                   obj.addProperty(entry.getKey(), (Character) entry.getValue());
                 else {
                   JsonElement jsonElement = gson.toJsonTree(entry.getValue());
                   if (jsonElement.isJsonArray()) {
                     obj.add(entry.getKey(), jsonElement.getAsJsonArray());
                   } else {
                     obj.add(entry.getKey(), jsonElement.getAsJsonObject());
                   }
                 }
               }
             }
             elementAdapter.write(out, obj);
           }

           @Override
           public ProcessLogPage read(JsonReader in) throws IOException {
             JsonElement jsonElement = elementAdapter.read(in);
             validateJsonElement(jsonElement);
             JsonObject jsonObj = jsonElement.getAsJsonObject();
             // store additional fields in the deserialized instance
             ProcessLogPage instance = thisAdapter.fromJsonTree(jsonObj);
             for (Map.Entry<String, JsonElement> entry : jsonObj.entrySet()) {
               if (!openapiFields.contains(entry.getKey())) {
                 if (entry.getValue().isJsonPrimitive()) { // primitive type
                   if (entry.getValue().getAsJsonPrimitive().isString())
                     instance.putAdditionalProperty(entry.getKey(), entry.getValue().getAsString());
                   else if (entry.getValue().getAsJsonPrimitive().isNumber())
                     instance.putAdditionalProperty(entry.getKey(), entry.getValue().getAsNumber());
                   else if (entry.getValue().getAsJsonPrimitive().isBoolean())
                     instance.putAdditionalProperty(entry.getKey(), entry.getValue().getAsBoolean());
                   else
                     throw new IllegalArgumentException(String.format(java.util.Locale.ROOT, "The field `%s` has unknown primitive type. Value: %s", entry.getKey(), entry.getValue().toString()));
                 } else if (entry.getValue().isJsonArray()) {
                     instance.putAdditionalProperty(entry.getKey(), gson.fromJson(entry.getValue(), List.class));
                 } else { // JSON object
                     instance.putAdditionalProperty(entry.getKey(), gson.fromJson(entry.getValue(), HashMap.class));
                 }
               }
             }
             return instance;
           }

       }.nullSafe();
    }
  }

  /**
   * Create an instance of ProcessLogPage given an JSON string
   *
   * @param jsonString JSON string
   * @return An instance of ProcessLogPage
   * @throws IOException if the JSON string is invalid with respect to ProcessLogPage
   */
  public static ProcessLogPage fromJson(String jsonString) throws IOException {
    return JSON.getGson().fromJson(jsonString, ProcessLogPage.class);
  }

  /**
   * Convert an instance of ProcessLogPage to an JSON string
   *
   * @return JSON string
   */
  public String toJson() {
    return JSON.getGson().toJson(this);
  }
}

