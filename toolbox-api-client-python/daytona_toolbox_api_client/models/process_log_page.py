# coding: utf-8

"""
    Daytona Toolbox API

    Daytona Toolbox API. The base URL comes from the sandbox's `toolboxProxyUrl` field (returned in sandbox DTO by the main Daytona API) plus the sandbox ID: `{toolboxProxyUrl}/{sandboxId}/{endpoint}`. Default for Daytona Cloud: `https://proxy.app.daytona.io/toolbox/{sandboxId}`.

    The version of the OpenAPI document: v0.0.0-dev
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from daytona_toolbox_api_client.models.process_log_frame import ProcessLogFrame
from pydantic import TypeAdapter
from typing import Optional, Set
from typing_extensions import Self

_JSON_ADAPTER = TypeAdapter(Dict[str, Any])

class ProcessLogPage(BaseModel):
    """
    ProcessLogPage
    """ # noqa: E501
    eof: Optional[StrictBool] = Field(default=None, description="EOF is true when the process is terminal and every retained frame has been returned.")
    first_available_cursor: Optional[StrictStr] = Field(default=None, serialization_alias="firstAvailableCursor")
    frames: List[ProcessLogFrame]
    next_cursor: Optional[StrictStr] = Field(default=None, description="NextCursor resumes strictly after the last returned frame. It is set even when Frames is empty so pollers can keep a stable position.", serialization_alias="nextCursor")
    truncated_head: Optional[StrictBool] = Field(default=None, description="TruncatedHead reports that the oldest frames of this process were evicted under a byte cap. Never silently skip: this is the honest signal.", serialization_alias="truncatedHead")
    additional_properties: Dict[str, Any] = {}
    __properties: ClassVar[List[str]] = ["eof", "firstAvailableCursor", "frames", "nextCursor", "truncatedHead"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return _JSON_ADAPTER.dump_json(self.to_dict()).decode()

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of ProcessLogPage from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        * Fields in `self.additional_properties` are added to the output dict.
        """
        excluded_fields: Set[str] = set([
            "additional_properties",
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each item in frames (list)
        _items = []
        if self.frames:
            for _item_frames in self.frames:
                if _item_frames:
                    _items.append(_item_frames.to_dict())
            _dict['frames'] = _items
        # puts key-value pairs in additional_properties in the top level
        if self.additional_properties is not None:
            for _key, _value in self.additional_properties.items():
                _dict[_key] = _value

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of ProcessLogPage from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "eof": obj.get("eof"),
            "first_available_cursor": obj.get("firstAvailableCursor"),
            "frames": [ProcessLogFrame.from_dict(_item) for _item in obj["frames"]] if obj.get("frames") is not None else None,
            "next_cursor": obj.get("nextCursor"),
            "truncated_head": obj.get("truncatedHead")
        })
        # store additional fields in additional_properties
        for _key in obj.keys():
            if _key not in cls.__properties:
                _obj.additional_properties[_key] = obj.get(_key)

        return _obj


