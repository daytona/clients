/*
 * Daytona Toolbox API
 * Daytona Toolbox API. The base URL comes from the sandbox's `toolboxProxyUrl` field (returned in sandbox DTO by the main Daytona API) plus the sandbox ID: `{toolboxProxyUrl}/{sandboxId}/{endpoint}`. Default for Daytona Cloud: `https://proxy.app.daytona.io/toolbox/{sandboxId}`.
 *
 * The version of the OpenAPI document: v0.0.0-dev
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package io.daytona.toolbox.client.model;

import java.util.Objects;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.Arrays;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonArray;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.TypeAdapter;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;

import java.util.HashMap;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Set;

import io.daytona.toolbox.client.JSON;

/**
 * KillProcessRequest
 */
@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", comments = "Generator version: 7.21.0")
public class KillProcessRequest {
  public static final String SERIALIZED_NAME_ESCALATE_AFTER_MS = "escalateAfterMs";
  @SerializedName(SERIALIZED_NAME_ESCALATE_AFTER_MS)
  @javax.annotation.Nullable
  private Integer escalateAfterMs;

  public static final String SERIALIZED_NAME_ESCALATE_TO = "escalateTo";
  @SerializedName(SERIALIZED_NAME_ESCALATE_TO)
  @javax.annotation.Nullable
  private String escalateTo;

  public static final String SERIALIZED_NAME_SIGNAL = "signal";
  @SerializedName(SERIALIZED_NAME_SIGNAL)
  @javax.annotation.Nullable
  private String signal;

  public KillProcessRequest() {
  }

  public KillProcessRequest escalateAfterMs(@javax.annotation.Nullable Integer escalateAfterMs) {
    this.escalateAfterMs = escalateAfterMs;
    return this;
  }

  /**
   * EscalateAfterMs escalates to EscalateTo if the process is still alive after the grace period. This is what makes kill() honest for TERM-trapping processes: the trap runs and is captured, and the process still dies.
   * @return escalateAfterMs
   */
  @javax.annotation.Nullable
  public Integer getEscalateAfterMs() {
    return escalateAfterMs;
  }

  public void setEscalateAfterMs(@javax.annotation.Nullable Integer escalateAfterMs) {
    this.escalateAfterMs = escalateAfterMs;
  }


  public KillProcessRequest escalateTo(@javax.annotation.Nullable String escalateTo) {
    this.escalateTo = escalateTo;
    return this;
  }

  /**
   * Get escalateTo
   * @return escalateTo
   */
  @javax.annotation.Nullable
  public String getEscalateTo() {
    return escalateTo;
  }

  public void setEscalateTo(@javax.annotation.Nullable String escalateTo) {
    this.escalateTo = escalateTo;
  }


  public KillProcessRequest signal(@javax.annotation.Nullable String signal) {
    this.signal = signal;
    return this;
  }

  /**
   * Get signal
   * @return signal
   */
  @javax.annotation.Nullable
  public String getSignal() {
    return signal;
  }

  public void setSignal(@javax.annotation.Nullable String signal) {
    this.signal = signal;
  }

  /**
   * A container for additional, undeclared properties.
   * This is a holder for any undeclared properties as specified with
   * the 'additionalProperties' keyword in the OAS document.
   */
  private Map<String, Object> additionalProperties;

  /**
   * Set the additional (undeclared) property with the specified name and value.
   * If the property does not already exist, create it otherwise replace it.
   *
   * @param key name of the property
   * @param value value of the property
   * @return the KillProcessRequest instance itself
   */
  public KillProcessRequest putAdditionalProperty(String key, Object value) {
    if (this.additionalProperties == null) {
        this.additionalProperties = new HashMap<String, Object>();
    }
    this.additionalProperties.put(key, value);
    return this;
  }

  /**
   * Return the additional (undeclared) property.
   *
   * @return a map of objects
   */
  public Map<String, Object> getAdditionalProperties() {
    return additionalProperties;
  }

  /**
   * Return the additional (undeclared) property with the specified name.
   *
   * @param key name of the property
   * @return an object
   */
  public Object getAdditionalProperty(String key) {
    if (this.additionalProperties == null) {
        return null;
    }
    return this.additionalProperties.get(key);
  }


  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    KillProcessRequest killProcessRequest = (KillProcessRequest) o;
    return Objects.equals(this.escalateAfterMs, killProcessRequest.escalateAfterMs) &&
        Objects.equals(this.escalateTo, killProcessRequest.escalateTo) &&
        Objects.equals(this.signal, killProcessRequest.signal)&&
        Objects.equals(this.additionalProperties, killProcessRequest.additionalProperties);
  }

  @Override
  public int hashCode() {
    return Objects.hash(escalateAfterMs, escalateTo, signal, additionalProperties);
  }

  @Override
  public String toString() {
    StringBuilder sb = new StringBuilder();
    sb.append("class KillProcessRequest {\n");
    sb.append("    escalateAfterMs: ").append(toIndentedString(escalateAfterMs)).append("\n");
    sb.append("    escalateTo: ").append(toIndentedString(escalateTo)).append("\n");
    sb.append("    signal: ").append(toIndentedString(signal)).append("\n");
    sb.append("    additionalProperties: ").append(toIndentedString(additionalProperties)).append("\n");
    sb.append("}");
    return sb.toString();
  }

  /**
   * Convert the given object to string with each line indented by 4 spaces
   * (except the first line).
   */
  private String toIndentedString(Object o) {
    return o == null ? "null" : o.toString().replace("\n", "\n    ");
  }


  public static HashSet<String> openapiFields;
  public static HashSet<String> openapiRequiredFields;

  static {
    // a set of all properties/fields (JSON key names)
    openapiFields = new HashSet<String>(Arrays.asList("escalateAfterMs", "escalateTo", "signal"));

    // a set of required properties/fields (JSON key names)
    openapiRequiredFields = new HashSet<String>(0);
  }

  /**
   * Validates the JSON Element and throws an exception if issues found
   *
   * @param jsonElement JSON Element
   * @throws IOException if the JSON Element is invalid with respect to KillProcessRequest
   */
  public static void validateJsonElement(JsonElement jsonElement) throws IOException {
      if (jsonElement == null) {
        if (!KillProcessRequest.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
          throw new IllegalArgumentException(String.format(java.util.Locale.ROOT, "The required field(s) %s in KillProcessRequest is not found in the empty JSON string", KillProcessRequest.openapiRequiredFields.toString()));
        }
      }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
      if ((jsonObj.get("escalateTo") != null && !jsonObj.get("escalateTo").isJsonNull()) && !jsonObj.get("escalateTo").isJsonPrimitive()) {
        throw new IllegalArgumentException(String.format(java.util.Locale.ROOT, "Expected the field `escalateTo` to be a primitive type in the JSON string but got `%s`", jsonObj.get("escalateTo").toString()));
      }
      if ((jsonObj.get("signal") != null && !jsonObj.get("signal").isJsonNull()) && !jsonObj.get("signal").isJsonPrimitive()) {
        throw new IllegalArgumentException(String.format(java.util.Locale.ROOT, "Expected the field `signal` to be a primitive type in the JSON string but got `%s`", jsonObj.get("signal").toString()));
      }
  }

  public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
    @SuppressWarnings("unchecked")
    @Override
    public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
       if (!KillProcessRequest.class.isAssignableFrom(type.getRawType())) {
         return null; // this class only serializes 'KillProcessRequest' and its subtypes
       }
       final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
       final TypeAdapter<KillProcessRequest> thisAdapter
                        = gson.getDelegateAdapter(this, TypeToken.get(KillProcessRequest.class));

       return (TypeAdapter<T>) new TypeAdapter<KillProcessRequest>() {
           @Override
           public void write(JsonWriter out, KillProcessRequest value) throws IOException {
             JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
             obj.remove("additionalProperties");
             // serialize additional properties
             if (value.getAdditionalProperties() != null) {
               for (Map.Entry<String, Object> entry : value.getAdditionalProperties().entrySet()) {
                 if (entry.getValue() instanceof String)
                   obj.addProperty(entry.getKey(), (String) entry.getValue());
                 else if (entry.getValue() instanceof Number)
                   obj.addProperty(entry.getKey(), (Number) entry.getValue());
                 else if (entry.getValue() instanceof Boolean)
                   obj.addProperty(entry.getKey(), (Boolean) entry.getValue());
                 else if (entry.getValue() instanceof Character)
                   obj.addProperty(entry.getKey(), (Character) entry.getValue());
                 else {
                   JsonElement jsonElement = gson.toJsonTree(entry.getValue());
                   if (jsonElement.isJsonArray()) {
                     obj.add(entry.getKey(), jsonElement.getAsJsonArray());
                   } else {
                     obj.add(entry.getKey(), jsonElement.getAsJsonObject());
                   }
                 }
               }
             }
             elementAdapter.write(out, obj);
           }

           @Override
           public KillProcessRequest read(JsonReader in) throws IOException {
             JsonElement jsonElement = elementAdapter.read(in);
             validateJsonElement(jsonElement);
             JsonObject jsonObj = jsonElement.getAsJsonObject();
             // store additional fields in the deserialized instance
             KillProcessRequest instance = thisAdapter.fromJsonTree(jsonObj);
             for (Map.Entry<String, JsonElement> entry : jsonObj.entrySet()) {
               if (!openapiFields.contains(entry.getKey())) {
                 if (entry.getValue().isJsonPrimitive()) { // primitive type
                   if (entry.getValue().getAsJsonPrimitive().isString())
                     instance.putAdditionalProperty(entry.getKey(), entry.getValue().getAsString());
                   else if (entry.getValue().getAsJsonPrimitive().isNumber())
                     instance.putAdditionalProperty(entry.getKey(), entry.getValue().getAsNumber());
                   else if (entry.getValue().getAsJsonPrimitive().isBoolean())
                     instance.putAdditionalProperty(entry.getKey(), entry.getValue().getAsBoolean());
                   else
                     throw new IllegalArgumentException(String.format(java.util.Locale.ROOT, "The field `%s` has unknown primitive type. Value: %s", entry.getKey(), entry.getValue().toString()));
                 } else if (entry.getValue().isJsonArray()) {
                     instance.putAdditionalProperty(entry.getKey(), gson.fromJson(entry.getValue(), List.class));
                 } else { // JSON object
                     instance.putAdditionalProperty(entry.getKey(), gson.fromJson(entry.getValue(), HashMap.class));
                 }
               }
             }
             return instance;
           }

       }.nullSafe();
    }
  }

  /**
   * Create an instance of KillProcessRequest given an JSON string
   *
   * @param jsonString JSON string
   * @return An instance of KillProcessRequest
   * @throws IOException if the JSON string is invalid with respect to KillProcessRequest
   */
  public static KillProcessRequest fromJson(String jsonString) throws IOException {
    return JSON.getGson().fromJson(jsonString, KillProcessRequest.class);
  }

  /**
   * Convert an instance of KillProcessRequest to an JSON string
   *
   * @return JSON string
   */
  public String toJson() {
    return JSON.getGson().toJson(this);
  }
}

